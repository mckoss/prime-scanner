# Progress on the drafts in submit/edits.yaml, which oeis_audit.py generates.
#
#   <item id>  <status>  [date]  [note]
#
# status: drafted    work exists, not yet submitted (item stays in its draft)
#         submitted  waiting on an OEIS editor (item leaves the script)
#         approved | declined | skip   closes the item
#
# The id is one of the `items:` entries under a draft in edits.yaml.
#
# `draft:<A-number>` is different: it records that the SEQUENCE has an open
# draft on OEIS. Draft state is behind a login, so the audit cannot see it --
# without a line here the script would happily offer edits that stack on top
# of a review already in progress.

draft:A023186   submitted  2026-09-08  rev 59, proposed; balanced-primes comment. Awaiting a reply to Michael S. Branicky re A054342, and Michel Marcus is waiting on the new sequence
b-file:A096265  drafted    2026-09-14  generated to submit/b096265.txt; all 68 terms confirmed by the scan
new:balanced    drafted    2026-09-14  generated to submit/edits.yaml under new_sequences
