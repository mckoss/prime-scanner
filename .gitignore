# Build artifacts
sieve
sieve-w*
reference/mod30
*.o
__pycache__/
*.pyc

# Parallel-run scratch (pgaps.py). Anything worth keeping has been merged up
# into <out>/{gap,lonely,aloof}.txt; <out>/frontier.txt is the resume point.
# per-worker candidates, checkpoints and logs -- deleted and rebuilt each round
shards/
# the round in flight; meaningless without its shards/ tree
round.txt
# the driver's PID, to keep two drivers off one output directory
lock
# oeis_audit.py's working copies of entries it re-fetches
oeis/audit/
